name: Deploy

on:
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    # Waits here for approval when the "production" environment requires it.
    environment: production
    steps:
      - uses: actions/checkout@v4
      - name: Deploy
        run: echo "Replace this step with your deployment command."
